-> por algum motivo do diabo, o redirecionamento de página não está funcionando (return session.isLoggedIn ? const MainScreenView() : const FirstAcess();)

-> POR ALGUM MOTIVO DO DIABO, o cookie não está chegando para o frontend